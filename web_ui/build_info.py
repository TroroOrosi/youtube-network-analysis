"""Replaced only in the clean source archive by scripts.release_production.

Do not obtain this from a runtime environment variable: an inherited/stale
variable must never make an old image look like the intended deployment.
"""
SOURCE_REVISION = 'unknown'
